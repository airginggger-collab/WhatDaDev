# HANDOFF — снимок состояния проекта

Обновлено: 2026-07-14 · версия **v0.89**
Цель файла: чтобы новая сессия (Claude или человек) за минуту поняла, где мы.

> **v0.81 — deliverable, не сайт:** продающая демо-презентация (7 слайдов, инфографика) в `deliverables/WhatDaDev_Презентация_Знакомство.{pdf,pptx,html}`. Слайд 1 — **обложка-титул** (navy, вордмарк + опоры «О нас»); дальше Проблема · ELMA365 · Coordo · Доказательство · Экосистема · CTA. Палитра — **моно-синяя корпоративная**, синий усилен: navy-«крышки» (обложка+CTA) + navy-заголовки контента `#123F73` + электрик-азур `#1668C0` акценты; оранжевого нет, WCAG AA. Идентичность дека отдельная от сайта — только демо; **сайт и ADR-0013/0014 не трогаем.** PPTX — Arial/Courier New, PDF/HTML — IBM Plex. Детали — `deliverables/README.md`, `docs/CHANGELOG.md` v0.81.

> **Правило:** каждое изменение кода/контента → обновить `docs/CHANGELOG.md` и этот файл. Без обновления доков изменение не считается завершённым.

> **Правило (v0.78):** любая поломка (сборка/деплой/скрипт/поведение) → разобрать что и почему, записать в журнал грабель `docs/LESSONS.md`, чтобы не повторять. Правило №7 в `CLAUDE.md`.

## Что это
Редизайн сайта **WhatDaDev** — внедрение ELMA365 + продукт «Экспресс внедрение (Coordo)». B2B-лидген, без e-commerce. Две воронки (Услуги / Продукты, 50/50), SEO-first.

## Где живёт
- Репозиторий: `git@github.com:airginggger-collab/WhatDaDev.git` (приватный, ветка `main`).
- Локально: `~/Desktop/WhatDaDev`.
- **Живой сайт:** https://whatdadev.airg-inggger.workers.dev/ (Cloudflare Workers Static Assets, автодеплой при пуше в `main`).
- Целевой домен: https://whatdadev.ru (пока старый WordPress).

## Стек
Astro 5 (статика, SSG) + `@astrojs/sitemap`. Стили — обычный CSS с CSS-переменными в `src/styles/global.css` (токены из `docs/04-design/tokens.json`); **Tailwind не используется**. **CMS: Sveltia на `/admin/` (ADR-0015)** — контент в `src/data/*.json` и markdown-коллекциях `src/content/{articles,glossary}`. Деплой — Cloudflare static assets (`wrangler.jsonc`, без SSR-воркера/адаптера).

## Что готово (live)
- **70 страниц (вкл. 404):** главная · `/coordo/` (лендинг с видео) · продукт + 3 контура + vs-разработка + pricing · модули + замещения · услуги + 8 направлений + разработка · 5 отраслей · 2 сравнения · пресс-центр + **20 статей** (волны 3.1+3.2) · **глоссарий /press/glossary/** (15 терминов + index) · **`/kz/` (KZ-лендинг, v0.85)** · ELMA365 · о компании · контакты · 404.
- **Без тире «—» во всём контенте (v0.84):** длинное тире читается как признак ИИ, убрано по всему `src/` (1296 → 0) переписыванием фраз, не удалением символа. Правило зафиксировано (CLAUDE.md №8, память `feedback-avoid-em-dash`). ЕН-тире «–» в числовых диапазонах сохранены.
- **Страница `/kz/` (v0.85):** отдельная KZ-воронка по мотивам лендинга ELMA365 — боли КЗ, суверенность/on-prem, локализация РК. Факты локализации и референсы (Air Astana/Bereke Bank) — в заглушках `{{...}}` (Фаза 0). Пока не в навигации (в sitemap попадает автоматически).
- **Визуал (v0.63, ADR-0013 — «уход от ИИшного дизайна»):** тёмный hero → светлый контент (сохранён); инженерно-издательская идентичность: сериф **Piazzolla** в H1/H2 + **Golos Text** в тексте + **IBM Plex Mono** в цифрах/подписях (self-hosted Fontsource, без Google CDN); тёплая бумага `#FAF8F2` + чернила, акцент — корпоративный синий `#1F5B99` (ADR-0014, v0.67); радиусы 6/4px, без pill; hover карточек — бордер + жёсткая тень. Главная: нумерованные фичи с реальными скриншотами ELMA365 вместо bento с фейковыми дашбордами; стат-таблица вместо кольца-метрики. Reveal — только при `html.js`, hero анимируется чистым CSS (без JS контент виден — SEO/LCP).
- **Личный кабинет (v0.69–v0.70, ADR-0015):** Sveltia CMS на `/admin/`, вход по GitHub-токену. Редактируемо: главная (`home.json`), цены Coordo (`pricing.json`), контакты (`contacts.json`), команда, направления, отрасли, **статьи и глоссарий** (markdown-коллекции `src/content/`, с черновиками). Инструкция — `docs/editing-cabinet.md`.
- **Вход в кабинет через GitHub в один клик (v0.73, ADR-0016):** OAuth-прокси — отдельный Worker `cms-auth/` (`whatdadev-cms-auth`), `base_url` в config.yml. Кнопка «Sign In with GitHub»; токен — запасной. ⚠️ Требует разового действия владельца: создать GitHub OAuth App + задеплоить воркер (`cms-auth/README.md`).
- **Визуальные шпаргалки токенов (v0.74):** `docs/04-design/tokens-cheatsheet.html` (полная, для Figma) и `docs/04-design/stitch-dark.html` (тёмная тема + промпт EN/RU для Google Stitch). Собраны из `tokens.json` + `global.css`, шрифты — относительными путями к `node_modules` (без base64). Справочники для дизайна, на сборку сайта не влияют.
- **Страница-инструкция кабинета (v0.72):** `/admin-guide/` — гайд входа в кабинет и редактирования на сайте (noindex, вне sitemap, не в nav). Ревью-воркфлоу нашёл и починил критичный пробел — пропущенное поле «Resource owner» при создании GitHub-токена (иначе заказчик застревал), a11y-ссылки, предупреждения о безопасности токена. Синхронно — `docs/editing-cabinet.md`.
- **Чистка мёртвого кода (v0.71):** аудит (5 финдеров + верификация) — исправлены битые CSS-переменные (`--radius-lg` → углы карточек команды, `--surface-alt2` indigo → нейтраль, `--bg-card`/`--transition-slow`), удалён мёртвый JS/CSS (videoPlaceholder, `.pc-lines`/`.pf-lines`, ghost id), осиротевшие ассеты (`demo.mp4` 772К, `og.svg`), 4× JSON-LD url без слэша. Плюс doc-drift после миграции (`.js`→`.json`, «CMS отложена»→Sveltia, токены/шрифты в ARCHITECTURE).
- **Последние улучшения (v0.64):** контент сверен с **https://whatdadev.ru/ — источник правды, утверждён заказчиком**. Убраны выдуманные факты («12 внедрений», «ответ за 2 часа», демо-цифры кейсов, «3 месяца поддержки»); «РФ и СНГ» → «РФ и КЗ»; Coordo — **3 готовых контура** (не 4) + «мобильное приложение в комплекте»; услуги — утверждённые 6 (вместо «Покупки лицензий» — «Поддержка и обучение»); модули — утверждённые описания + Коннектор RabbitMQ; преимущества на главной — дословно с утверждённого сайта; карьера на `/contacts/` (iperushev@whatdadev.ru).
- **Устаревшее (v0.63):** новая идентичность (см. выше) + фиксы: соцссылки `/press/` больше не `href="#"`; форма `/contacts/` вместо фейкового success ведёт в Telegram/WhatsApp с предзаполненной заявкой.
- **Устаревшее (v0.62):** Дизайн-аудит по чек-листу основ — бургер `.nav-toggle` тач-зона 44×44, `.price` отступ 10→8px, форма `/contacts/` с маркерами обязательных полей (`*` + подсказка).
- **Устаревшее (v0.61):** QA-аудит `/products/express/` — фикс контраста hero-дашборда (`.dash{color:var(--text)}`: заголовок и KPI «2 дня/14/0» были near-white на белом → теперь читаются; глобально для всех `.dash`), JSON-LD `url`/`offers.url` приведены к виду со слэшем.
- **Устаревшее (v0.60):** Оптимизация скорости — обложки −77% (848→194KB, WebP 800px), `_headers` Cloudflare кэш (images 1 нед, `/_astro/*` 1 год), `preload="none"` на видео (−7.4MB при загрузке главной/coordo).
- **Устаревшее (v0.59):** `/press/`, `/services/`, `/industries/` — обложки карточек: чистые скрины ELMA365 без цветного оверлея + тёмный градиент снизу. Соцкарточки `/press/`: убраны цветные border-top, правильные brand-иконки (Telegram/Instagram/YouTube/TikTok/VK) с круглым фоном.
- **Устаревшее (v0.58):** `/services/` и `/industries/` — редизайн в стиле `/press/`: featured-карточка + цветные обложки + grid. `/press/` — блок соцсетей (5 платформ). `/coordo/` — voice-card, валюты, ядро. `/services/vnedrenie/` — timeline 6 шагов.
- **Устаревшее (v0.57):** `/press/` — featured-карточка + цветные обложки по тегу + фильтры + блок соцсетей (5 платформ). `/coordo/` — voice-card с контрастом, исправлен переключатель валют, выровнено ядро. `/services/vnedrenie/` — timeline 6 шагов с описаниями.
- **SEO:** sitemap, robots, JSON-LD (Organization, WebSite, SoftwareApplication+Offer, Service, FAQPage, BreadcrumbList), canonical, OG/Twitter, favicon.

## Решения — см. `docs/08-decisions/` (ADR 0001–0016)
50/50 две воронки · тёмный hero → светлый · без красного · SEO-first · база знаний в Git · CMS — Sveltia на `/admin/` (ADR-0015, сняла ADR-0006) · утв. структура · без e-commerce · free-first своими силами · нейминг «Экспресс внедрение (Coordo)» · ~~токены v1 (indigo + Inter)~~ → **токены v2: уход от ИИшного дизайна — Piazzolla + Golos + Plex Mono, акцент — корпоративный синий (ADR-0014)**.

## Сделано 2026-06-20 — чистка репозитория
- Из git-трекинга убраны ~24MB случайно закоммиченных бинарников в **корне** репо (Frame 16.png, Frame 659.jpg, screencapture-cursor-*.png, «👨🏼_💻 Team - 3/4» jpg/pdf, 6762bb55…take.jpeg): `git rm --cached`, физически перенесены в новую gitignored-папку `_local-assets/` (остаются на диске).
- `.gitignore` обновлён: блокирует бинарники в корне (`/*.png /*.jpg /*.jpeg /*.pdf /*.mov /*.svg`) и папку `_local-assets/`.
- Правило закреплено в README и CLAUDE.md: никаких бинарников в корне — курируемые ассеты в `references/`/`deliverables/`, мусор/черновики в `_local-assets/`.

## Бэклог инфры (известно, не срочно)
- 🟡 Эти ~24MB всё ещё лежат в **старой git-ИСТОРИИ** — `git rm --cached` уменьшил рабочее дерево, но не `.git`. Чтобы реально ужать репозиторий, нужно переписать историю (`git filter-repo` + force-push в `main`). Делать осознанно (приватный репо, один автор) — отдельная задача.

## QA-бэклог (найдено в аудите v0.61, не блокеры)
- 🟡 **Форма `/contacts/` без CRM** — отправки в ELMA365 пока нет (Фаза 0). С v0.63 сабмит честно предлагает Telegram/WhatsApp (WhatsApp — с предзаполненным текстом заявки), фейкового «заявка принята» больше нет.
- 🟡 **`public/og.png` в старой айдентике** (indigo/Inter) — перегенерировать под ADR-0013.
- 🟢 **Иерархия заголовков** на `/products/express/`: H1 → H3 (заголовок hero-дашборда) до первого H2 — мелкий a11y/SEO-нюанс.
- 🟢 **FAQ-аккордеон** (`Base.astro`): кнопки имеют `aria-expanded`, но панели без `id`/`aria-controls`/`role` — связка для screen-reader неполная.
- 🟢 **Валидация полей формы:** `tel` принимает любой ввод, у `message` нет `maxlength` — безопасно сейчас, но нужна серверная валидация/санитайз при подключении CRM.
- ⚪ **Back-to-top** — проверить вручную на живом сайте (логика по коду верна; превью-сэндбокс не двигает viewport).
- ⚪ **Кольцо-метрика (Base.astro `.fg`/`--p` + CSS `.ring .fg`)** — инертный каркас (guarded, не ломает), разметки `.ring` нет. Оставлен под Фазу 0 (реальные цифры кейсов); удалить или доподключить при решении по метрике.

## Что осталось — Фаза 0 (нужен заказчик)

### Данные
🔴 Реальные **цифры кейсов** (с v0.64 выдуманных цифр на сайте нет; демо-дашборд `/products/express/` помечен «демо-данные»)
🔴 **Интеграция формы с ELMA365** (как лиды попадают в CRM, кто настроит)
🔴 Описания **узких модулей** (Аудит СМК, Экспорт объектной модели, Связанные процессы, Визуализация файлов)
🔴 Логотип в векторе, согласия клиентов на логотипы/отзывы. ✅ Фото Перушева добавлено (v0.75) — `/team/perushev.jpg`, на `/company/about/` и главной
🔴 **KZ-локализация для `/kz/`** (заглушки `{{...}}`): что реально поддерживаем — ЭЦП/NCA РК и eGov, соответствие закону РК, тенге/1С, приоритетные отрасли РК
🔴 **Право упоминания референсов ELMA365 в КЗ** (Air Astana, Bereke Bank) на `/kz/` — согласовать формулировку
🟡 **Линковка `/kz/` в навигацию** (шапка/футер) — решить, когда наполним фактами. Партнёрская регистрация ELMA365 в РК — см. `docs/strategy-kz-linkedin.md`

### Страницы Phase 0 (не построены — ждут контента)
- **Услуги:** `/services/autstaffing`, `/audit`, `/consulting`, `/obuchenie`, `/litsenzii`
- **Модули:** `/products/modules/audit-smk`, `/export-model`, `/linked-processes`, `/file-management`
- **Компания:** `/company/success-stories`, `/clients`, `/partners`, `/reviews`, `/brand`, `/requisites`
- **Пресс:** `/press/knowledge-base`, `/reviews`, `/social`, `/glossary`

Трекер и опросник: `docs/00b-discovery.md`, `deliverables/WhatDaDev_Опросник_Фаза0.docx`.

## Как продолжить
- Полный контекст: `docs/README.md` (порядок чтения 00→17) + `docs/CHANGELOG.md`.
- Править сайт руками без ИИ: `docs/editing-guide.md`.
- Тексты страниц: `docs/12-copy/`, данные: `src/data/*.json`.
- Коммит-автор обязателен: `airg.inggger@gmail.com` / `airginggger-collab`.

## Команды
```
npm install        # один раз
npm run dev        # локальный предпросмотр → http://localhost:4321
npm run build      # сборка → dist/
npm run preview    # предпросмотр собранного dist/
git push           # публикация (Cloudflare соберёт сам)
```
